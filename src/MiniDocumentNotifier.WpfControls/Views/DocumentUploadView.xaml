<UserControl x:Class="MiniDocumentNotifier.WpfControls.Views.DocumentUploadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MiniDocumentNotifier.WpfControls.Views"
             xmlns:converters="clr-namespace:MiniDocumentNotifier.WpfControls.Converters"
             mc:Ignorable="d"
             d:DesignHeight="220" d:DesignWidth="400">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <StackPanel Margin="20">
        <TextBlock Text="Selected file" Margin="0,0,0,4" />
        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding DocumentName, Mode=OneWay}" IsReadOnly="True" />
            <Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseCommand}" Margin="8,0,0,0" />
        </Grid>

        <TextBlock Text="Document type" Margin="0,0,0,4" />
        <ComboBox ItemsSource="{Binding AvailableTypes}" SelectedItem="{Binding SelectedType}" Margin="0,0,0,12" />

        <TextBlock Text="{Binding StatusMessage}" Foreground="Gray" Margin="0,0,0,12" />

        <ProgressBar IsIndeterminate="{Binding IsUploading}" Height="4" Margin="0,0,0,12"
                     Visibility="{Binding IsUploading, Converter={StaticResource BoolToVisibilityConverter}}" />

        <Button Content="Upload" Command="{Binding UploadCommand}" HorizontalAlignment="Right" Width="100" />
    </StackPanel>
</UserControl>